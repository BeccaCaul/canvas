#include <stdio.h>
#include <stdlib.h>

int main(void)
{
  /* Declare a pointer to int */
  /* Declare a pointer to float */
  
  /* use malloc to allocate an array of 10 ints and store in your pointer to int */
  /* use malloc to allocate an array of 10 floats and store in your pointer to float */
  
  /* use a for loop to set all the items in the int array to 0 */
  /* use a for loop to set all the items in the float array to 5 */
  
  /* print out the int array */
  /* print out the float array */
  
  /* don't forget to return */
}
