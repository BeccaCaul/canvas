/* 
 * use the following commands to compile:
 * gcc -Wall -Werror -Wextra -ansi -pedantic -o compiled.exe compiled.c
 */
#include <stdio.h> /* for Input/Output */

int main(void)
{
    printf("Compiling complete!\n");
    return 0;
}