#include <stdio.h>

int main(void)
{
  /* Declare 5 variables of the type integers named exactly height, length, width, volume, weight */
  
  /* Assign any number you like to height, length and width */
  
  /* Assign volume by calculating it by using the formula height * length * width */
  
  /* To calculate weight is (volume + 165) / 166 */
  
  
  /* Do not remove these, this prints out the information to the screen */
  printf("Dimension: %i, %i, %i,\n", length, width, height);
  printf("Volume (cubic inches): %i\n", volume);
  printf("Dimensional weight (pounds): %i\n", weight);
  
  return 0;
}